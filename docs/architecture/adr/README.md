# ADR directory

Accepted architecture decisions are currently consolidated in `../03_ADR_log.md`.

Create a separate ADR file in this directory only after an explicit decision to split an ADR out of the consolidated register. Keep `03_ADR_log.md` as the navigation/index point and preserve the architecture package's normativity and precedence rules.
