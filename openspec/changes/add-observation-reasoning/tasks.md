## 1. Reasoning Contracts and Input Boundary

- [x] 1.1 Create the `app.reasoning` package and strict immutable framework-neutral contracts for run identity, a Metric/Alert-only compact Observation/Lens semantic context, unavailable Lens metadata that reuses the accepted strict `StructuredReason` value contract, limitation variants, structured evidence references/catalog entries, finding, hypothesis, and knowledge-isolated overall-state requests/completions, frozen findings, hypotheses, ObservationAnalysisResult 1.0, and typed execution outcomes; add concise public docstrings and reject unknown fields.
- [x] 1.2 Define one framework-neutral Observation Reasoning agent protocol with explicit finding, hypothesis, and overall-state methods, reusing the existing KnowledgeRetriever port without importing PydanticAI, OpenRouter, persistence, or provider types.
- [x] 1.3 Implement the correlated input boundary for native strict Metric, Alert, and Relationship contracts, including exact usable variants, completed-insufficient Metric projection to unavailable `StructuredReason(code="insufficient_data")`, exact preservation of caller-supplied non-empty reason code and optional component for failed/non-usable Metric and Alert Lenses, an exact usable/unavailable partition of supported semantic-context Lens identities, explicit rejection of contexts containing Log or unsupported Lens types before partitioning, duplicate/overlap/unknown/missing Lens rejection, run-identity correlation, Relationship uniqueness, supplied ordering, and zero-usable rejection.
- [x] 1.4 Add contract/input tests for every valid Metric/Alert variant and invalid combination, UUID identity preservation, semantic-context field exclusion, explicit Log/unsupported-context rejection, failed/insufficient separation, exact preservation of representative Metric and Alert unavailable reason codes/components, rejection of missing/empty codes and extra/free-text reason fields, exact Lens-scope partition enforcement, duplicate/overlap/unknown/missing Lens and cross-run rejection, empty usable scope, and absence of raw/provider/infrastructure fields.

## 2. Evidence Catalog and Deterministic Limitations

- [x] 2.1 Implement the typed `source_type + source_id + locator` reference resolver over exact immutable validated source artifacts, including strict locator key/index validation and exact-one-source resolution.
- [x] 2.2 Implement deterministic fine-grained catalog projection and transient ordered IDs for admitted Metric current/reference/History semantics and evidence, Alert records/aggregates/comparisons/findings/importance, and Relationship applicability/state/evidence, while excluding envelope, provenance, reasons, unavailable data, knowledge, and diagnostics.
- [x] 2.3 Implement deterministic limitation projection in Observation Lens order for unavailable, completed-insufficient Metric, and partial usable Lens inputs using `missing_lens_evidence`, `insufficient_lens_evidence`, and `partial_lens_analysis`, preserving only the allowed partial component.
- [ ] 2.4 Add exhaustive catalog/resolver tests for optional-section presence and omission, all three source types, stable order, transient-ID translation, exact resolution, malformed locators, source ambiguity, and exclusion of non-evidence fields; add limitation tests for all mappings, ordering, empty output, and immutability.

## 3. Finding Freeze and Final Result Validation

- [x] 3.1 Implement deterministic phase-one output validation that resolves unique supplied catalog IDs, rejects unknown/duplicate references and duplicate IDs, and freezes the exact ordered finding IDs, statements, and structured references without a knowledge channel.
- [x] 3.2 Implement hypothesis validation against the frozen finding set and exact union of KnowledgeReference values returned by this run, including unique IDs/references, mandatory non-empty `supported_by` and `knowledge_refs`, and rejection of unsupported fields or invented grounding.
- [x] 3.3 Implement strict overall-state completion validation for the three accepted values with no findings, hypotheses, limitations, references, rationale, or other model-authored fields.
- [x] 3.4 Implement the sole ObservationAnalysisResult 1.0 builder that copies correlated UUID identity, frozen findings, validated hypotheses, isolated overall state, and deterministic limitations while excluding all transient catalog, retrieval, prompt, provider, model, and diagnostic data.
- [ ] 3.5 Add builder/freeze/state tests for zero and multiple findings/hypotheses, cross-Lens references, exact snapshot preservation, unknown evidence/finding/knowledge rejection, uncertainty with findings, forbidden taxonomy/confidence/recommendation fields, strict serialization, and compatibility with the existing ObservationAnalysisResult persistence envelope without performing persistence.

## 4. Three-Invocation Application Execution

- [x] 4.1 Implement the deterministic Reasoning executor stage order from correlated input through catalog/limitations, one evidence-only finding invocation, immutable freeze, optional retrieval-enabled hypothesis invocation, one knowledge-isolated overall-state invocation, and final build; skip hypothesis model work for empty findings while still invoking overall-state assessment.
- [x] 4.2 Compose exactly one BoundedRetrievalExecutor per non-empty frozen finding set and enforce the consumer refinement rule: candidate items from either call remain available, refinement requires a non-empty first result plus unresolved gap, empty/failure/timeout permits only an independent second call, and no executor replacement can reset the two-call budget.
- [x] 4.3 Build the overall-state request afresh after hypothesis completion using only Observation evidence, catalog, frozen findings, and deterministic limitations; prove that hypotheses, retrieved content/references, retrieval outcomes/ledger/tool, and hypothesis model history have no data path into the invocation.
- [x] 4.4 Normalize model failure, timeout, request-limit exhaustion, tool-policy violations, and invocation/result validation failures into the approved fail-closed behavior with fixed safe code/component output including `overall_state_phase`, no partial ObservationAnalysisResult, and no diagnostic leakage; propagate caller cancellation unchanged with no typed artifact or retry, and keep retrieval empty/failure/timeout non-fatal when hypothesis reasoning completes validly.
- [ ] 4.5 Add fake-driven executor tests for exact invocation order and visibility, retrieval unavailable before freeze, empty-finding hypothesis skip, knowledge-isolated overall state, zero/one/two calls, valid refinement and independent second call, both-call knowledge grounding, all retrieval outcomes, attempted budget reset, invalid tool policy, every typed Reasoning failure, unchanged cancellation, and absence of persistence/report/lifecycle/API side effects.

## 5. Bounded PydanticAI Adapter

- [x] 5.1 Implement the injected-model PydanticAI adapter as three explicit typed invocations with separate adapter-private prompts and projections: finding formation exposes no tools or knowledge, hypothesis formation exposes only the accepted retrieval tool for non-empty findings and treats returned statements as untrusted data, and overall-state determination exposes only Observation evidence with no hypothesis or knowledge channel.
- [x] 5.2 Add application-observed model wrappers and usage limits enforcing one finding request, at most three hypothesis requests, one overall-state request, at most one sequential retrieval call per hypothesis model response, five total requests at maximum, zero framework retries, no continuation after a final-slot tool call, and configured timeout/max-output values on every request.
- [x] 5.3 Translate PydanticAI model/tool/output failures into framework-neutral typed outcomes without exception, prompt, message, provider, retrieved-content, or credential leakage; propagate caller cancellation and preserve the run-scoped retrieval executor's cancellation/budget semantics.
- [ ] 5.4 Add deterministic PydanticAI test/function-model tests for exact invocation projections and ordering, structured outputs, no finding tools, skipped empty-finding hypothesis work, valid zero/one/two-call trajectories, refinement eligibility, invalid/parallel/over-budget tool requests, isolated overall-state input, per-invocation and total request ceilings, zero retries, timeout/failure/invalid completion mapping, reference validation, and framework-type isolation.

## 6. OpenRouter Production Configuration

- [x] 6.1 After approval of this plan's dependency change, update the backend dependency to `pydantic-ai-slim[openrouter]>=2,<3` and refresh only the backend lockfile; verify no unrelated package or stack change is introduced.
- [x] 6.2 Add private OpenRouter settings for optional SecretStr credential, default model `openai/gpt-5.6-terra`, positive 120-second request timeout, positive 12,288 maximum completion tokens, fallback enabled by default, and ordered duplicate-free provider configuration; require exactly one provider when fallback is disabled without making unrelated application startup require a credential.
- [x] 6.3 Implement native OpenRouterModel/OpenRouterProvider composition and inject the resulting model into all three Reasoning invocations, including configured provider order/fallback request policy and safe composition failure for missing credentials; add no preset, attribution, native web search, prompt cache, fallback-model list, or provider-specific domain contract.
- [x] 6.4 Document the new environment variables with placeholders in `.env.example` and update focused development guidance for model switching, default routing, and strict one-provider pinning without committing a credential.
- [ ] 6.5 Add settings/composition tests for defaults, arbitrary positive timeout/token overrides, model override, routing enabled with zero/multiple ordered providers, strict disabled-fallback validation, duplicate/blank provider rejection, missing/blank secret handling, exact provider policy projection, and secret absence from errors/representations.

## 7. Integration and Final Verification

- [ ] 7.1 Add one representative end-to-end in-memory reasoning test from mixed Metric/Alert/Relationship inputs through evidence catalog, deterministic limitations, three isolated fake model invocations, injected fake retrieval, frozen findings, grounded hypothesis, knowledge-isolated overall state, and strict ObservationAnalysisResult serialization.
- [ ] 7.2 Add degraded-path integration tests for partial and unavailable Lenses, completed-insufficient Metric visibility in final limitations, empty findings with skipped hypothesis model work, empty or failed retrieval with no hypotheses, and failure in each required invocation producing no result.
- [x] 7.3 Run focused backend tests, Ruff linting, and Ruff formatting checks for the capability and resolve all failures; optionally run a manually credentialed OpenRouter smoke test without making live access or credentials a CI requirement.
- [x] 7.4 Run `make check` and report any failures accurately before archive or pull-request work.
