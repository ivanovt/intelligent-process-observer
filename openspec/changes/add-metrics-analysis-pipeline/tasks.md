## 1. Framework-neutral Metric contracts and ports

- [x] 1.1 Create the minimal `backend/src/app/metrics/` package and strict/frozen execution models in `contracts.py`, reusing UUID for observation/run identities and non-empty strings for Lens/metric/unit identities; test exact primitive compatibility, immutability, UTC `analysis_window.from/to`, offset uniqueness/order, and rejection of aliases/unknown fields in `backend/tests/test_metric_analysis_contracts.py`.
- [x] 1.2 Define framework-neutral discriminated provider, preparation, semantic, optional-tool, agent-completion, ledger, reference, History, and infrastructure-failure outcomes in `contracts.py`; define strict `MetricAgentUsableRequest` and `MetricAgentInsufficientRequest` models (or an equivalently strict union) with their exact non-overlapping field sets, including the ordered strict descriptor tuple for `spike`, `oscillation`, and `stuck_signal`; ensure no SQLAlchemy, PydanticAI, Prometheus client, or provider-model type leaks into them and add import/boundary assertions.
- [x] 1.3 Define `MetricSeriesProvider`, `MetricsAnalysisAgent`, and `MetricHistoryReader` protocols in `ports.py` with immutable single-scope inputs and typed outcomes; add fake-based tests proving provider queries and dataset selectors cannot be authored by the agent.
- [x] 1.4 Define the strict four-variant MetricAnalysisResult 1.0 model union and all nested evidence/semantic/reason models in `contracts.py`, including finite values, exact omission/co-occurrence/correlation rules, identity types, `from/to`, the three fixed `MetricFailedError` code/message variants, and minimal failed shape; add focused contract tests before building pipeline code.

## 2. Deterministic preparation and mandatory evidence

- [x] 2.1 Implement UTC normalization, closed-window validation, ascending ordering, duplicate rejection, and non-finite removal in `preprocessing.py` as a role-neutral preparation outcome; test good, degraded, insufficient, out-of-window, and duplicate malformed inputs without embedding current/reference terminal policy in the pure function.
- [x] 2.2 Implement arithmetic mean, population standard deviation, finite extrema, and elapsed-second OLS slope without a new dependency; test exact known values, irregular timestamps, constant values, and numerical finiteness.
- [x] 2.3 Implement the exact `good|degraded|insufficient` classifier with minimum three finite distinct-timestamp samples and no cadence/gap inference; test every boundary and confirm insufficient output contains no fabricated mandatory evidence.

## 3. Deterministic mandatory semanticization

- [x] 3.1 Implement ADR-153 scale, normalized fitted-window trend change, OLS-residual variability, and constant-scale behavior in `semantics.py`; keep the implementation independent of Lens-specific thresholds and agent output.
- [x] 3.2 Add table-driven tests for values immediately below, exactly at, and immediately above trend thresholds `0.05/0.15/0.35` and variability thresholds `0.05/0.15`, including signed, near-zero, constant, increasing, and decreasing series.

## 4. Optional deterministic tool registry

- [x] 4.1 Implement the framework-neutral registry and typed success/not-applicable/failure/timeout/rejection outcomes in `tools.py`, exposing exactly `spike`, `oscillation`, and `stuck_signal`; test that drift/unregistered tools are unavailable and every tool is bound to one immutable prepared current dataset.
- [x] 4.2 Implement Spike with minimum five samples, median/MAD modified-z `>3.5`, and the exact MAD-zero deviation branches/evidence; test normal present/absent, zero-deviation absent, sparse-deviation present, excessive-deviation unknown, score omission in fallback, and not-applicable.
- [x] 4.3 Implement Oscillation with minimum eight samples, OLS residuals, strict 5%-scale deadband, significant residual/sign-change rules, and deterministic ratio evidence; test present, absent, all-zero absent, insufficient-significant unknown, threshold boundaries, and not-applicable.
- [x] 4.4 Implement Stuck Signal with minimum five samples, exact consecutive equality, earliest-longest tie-break, and 80% share; test present/absent/not-applicable and document/test that it detects repeated provider values rather than proving physical sensor failure.
- [x] 4.5 Test the exact optional property/evidence matrix: not evaluated and not-applicable omit both and do not cause partial; successful present/absent/unknown require both and do not cause partial; failed/timeout/rejected omit successful public fields and contribute the approved incompleteness behavior.

## 5. Reference-period comparison

- [x] 5.1 Implement exact equal-duration backward-offset window derivation and independent per-offset reference preparation in `references.py`; test zero, one, and multiple configured offsets, configured-order preservation, and no default/baseline semantics.
- [x] 5.2 Implement the symmetric relative-level formula and 0.05 higher/lower/similar tolerance plus ordered trend-rate and variability relations; test faster/slower/same, higher/lower/similar, direction same/different, and every not-comparable case.
- [x] 5.3 Build correlated semantic/evidence reference items containing exact offset/window, reference descriptors/statistics, and relative-level value; test unique paired offsets, matching order/windows, and rejection of `previous_period`.
- [x] 5.4 Map acquisition failure/timeout, duplicate timestamps, out-of-window samples, and analytical insufficiency after non-finite filtering for one reference to an omitted comparison plus partial `reference_unavailable/reference_periods`; retain specific cause/offset diagnostics, preserve all successful offsets, create no unknown placeholder, and test every cause, current-insufficient exemption, and that malformed reference data never fails an otherwise usable current analysis.

## 6. Persisted-result History analysis

- [x] 6.1 Implement pure History transition/direction/pattern analysis in `history.py` using accepted defaults/override precedence and ADR-159/ADR-160 exactly: pair-relative inclusive near-zero guard without epsilon, inclusive relative stable band, unknown exclusion, ADR-023 direction dominance unchanged, stable-neutral directional projection, directional run compression, `direction_changes=max(0,len(runs)-1)`, and fixed pattern priority. Add table-driven tests using full transition names for zero/zero, exact and just-outside near-zero boundaries, exact and just-outside `±t`, fewer than two classifiable transitions, unknown-denominator exclusion, stable participation in sustained shares, priority, direction-change evidence, and every required sequence: `[increasing,increasing]`, `[stable,stable]`, `[increasing,decreasing]`, `[increasing,increasing,decreasing,decreasing]`, `[increasing,stable,decreasing]`, `[increasing,decreasing,increasing]`, `[decreasing,increasing,decreasing]`, `[increasing,increasing,decreasing,decreasing,increasing]`, `[increasing,stable,increasing]`, `[increasing,unknown,decreasing]`, and `[stable,unknown,stable]`.
- [x] 6.2 Implement ADR-158 candidate validation, overlap eligibility, total event-time ordering, newest-lookback selection, and oldest-first `run_ids`; test overlapping windows, strict earlier-end cutoff, equal-end/start tie-breaks, late persistence, and completion-order independence.
- [x] 6.3 Add typed History stage outcome mapping: normal absence and analytical unknown remain successful, unexpected deterministic computation failure yields partial `history_analysis_failed/history`, and repository/session failure propagates as infrastructure error; add pipeline-facing unit tests for all three distinctions.

## 7. PydanticAI Metrics Agent adapter

- [x] 7.1 After explicit dependency approval, add `pydantic-ai-slim>=2,<3` without provider extras to `backend/pyproject.toml` and update `backend/uv.lock`; verify no OpenRouter/OpenAI/provider extra or model default is introduced.
- [x] 7.2 Implement the injected-model adapter in `backend/src/app/infrastructure/agents/pydantic_ai_metrics.py` using strict `MetricAgentCompletion{state="completed"}` and the two exact framework-neutral projections: good/degraded receives identity, window, objectives, quality, mandatory evidence, trend/variability, the fixed ordered tool-descriptor tuple, and opaque `dataset_ref`; insufficient receives identity/window/quality only and exposes no tools. Ensure neither projection contains raw/compact series, provider address/query, reference data, or History data, and keep natural-language prompt/tool prose adapter-private.
- [x] 7.3 Implement the application-owned three-slot/each-tool-once ledger and adapter enforcement for success, not-applicable, failure, timeout, duplicate, unregistered, parallel, and fourth-call requests; configure zero PydanticAI tool/output validation retries, no model-retry hook, and a hard four-model-request limit; guarantee validation failure creates no corrective request, a duplicate/fourth call never executes, no fifth model request occurs, and not-applicable/successful unknown do not cause partial.
- [x] 7.4 Map good/degraded agent failure, timeout, invalid completion, and protocol rejection to usable partial optional-analysis outcomes while retaining earlier valid deterministic tool results; map insufficient-quality agent failures to operational diagnostics plus deterministic no-optional completion.
- [x] 7.5 Add deterministic adapter tests with injected test/function models for exact good/degraded usable requests and descriptor order/literals, the exact identity/window/quality-only insufficient request with no tools/dataset reference, zero calls, all three calls, duplicate rejection, fourth rejection, zero tool/output validation retries, the four-request ceiling/no fifth request, invalid/unacceptable completion mapping, timeout/failure translation, insufficient-quality resilience, and absence of PydanticAI types outside infrastructure.

## 8. Deterministic result building

- [x] 8.1 Implement `result_builder.py` as the sole constructor for completed-sufficient, completed-insufficient, partial, and failed MetricAnalysisResult variants; correlate immutable identity, exact window, context-derived `prometheus` provenance, current/reference/History semantics/evidence, optional tool projections, and LensRun reason; map typed mandatory-stage failures to the three fixed public code/message pairs without diagnostic leakage.
- [x] 8.2 Add exhaustive builder tests for exact identity primitive reuse, finite values, strict unknown-field rejection, optional property/evidence co-occurrence, reference/history paired sections, reason precedence and deterministic component selection, every fixed failed-error mapping, failure-before-provider-success provenance and injected-clock generation time, completed-insufficient forbidden fields, minimal-failed forbidden fields, `analysis_window.from/to`, and rejection of `start/end`, raw samples, dataset references, ledgers, diagnostics, prompts, or framework/provider objects.

## 9. Pipeline application service and terminal mapping

- [x] 9.1 Implement `pipeline.py` against the completed result-builder contract with the approved deterministic stage order for one already-running immutable LensRun, fake-injected ports, current mandatory failure, completed sufficient, and completed insufficient paths; test that it never creates top-level runs or mutates Metric scope.
- [x] 9.2 Implement exact optional/reference/History incompleteness aggregation and public precedence `reference_unavailable > history_analysis_failed > optional_analysis_failed`; fix components to `reference_periods` and `history` for the first two reasons, and for optional failure select `metrics_agent` when any agent/protocol failure exists or otherwise the earliest failed/timed-out registered tool by attempt ordinal. Test reference+optional, History+optional, two tool failures, tool failure followed by agent failure, duplicate/fourth rejection, not-applicable-only, successful-unknown-only, and preservation of all secondary operational diagnostics outside MetricAnalysisResult.
- [x] 9.3 Map current acquisition, malformed preparation, and unexpected mandatory-analysis failures to typed failed-result builder inputs; ensure the same malformed conditions in one reference cannot fail a usable current analysis and History repository/transaction infrastructure failure escapes without a fabricated analytical terminal result; add focused pipeline tests.

## 10. Existing runtime-persistence integration

- [x] 10.1 Extend only `backend/src/app/infrastructure/persistence/repository.py` with the bounded same-`observation_id + lens_id` eligible Metric History read, excluding current/failed/insufficient results and implementing ADR-158 chronology without a new repository/table/migration; add PostgreSQL query/filter/order/lookback integration tests.
- [x] 10.2 Compose History read, terminal LensRun transition, and correlated Metric artifact persistence inside the existing caller-owned transaction; test reason/status correlation and completed/partial/failed round trips through `LensAnalysisResultInput`.
- [x] 10.3 Add PostgreSQL integration tests that force flush/commit failure and verify rollback of both terminal LensRun state and artifact, infrastructure-error propagation, and no fabricated persisted failed result when the backend itself is unavailable.

## 11. Documentation and verification

- [x] 11.1 Add concise developer-facing documentation/docstrings for the internal provider/model injection points, accepted Metric result contract, and future Prometheus/provider composition boundary without duplicating architecture documents.
- [x] 11.2 Run focused backend tests for contracts, preparation, semantics, tools, references, History, adapter, pipeline, builder, and persistence; report failures accurately.
- [x] 11.3 Run `openspec validate add-metrics-analysis-pipeline --strict` and the repository `make check`; do not archive, push, create a PR, or start top-level orchestration in this task.
