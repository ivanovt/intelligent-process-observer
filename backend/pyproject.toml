[project]
name = "intelligent-process-observer-backend"
version = "0.1.0"
description = "Backend for Intelligent Process Observer."
requires-python = ">=3.13"
dependencies = [
    "alembic",
    "fastapi",
    "httpx>=0.28,<1",
    "pydantic-settings",
    "pydantic-ai-slim[openrouter]>=2,<3",
    "psycopg[binary]",
    "sqlalchemy",
    "uvicorn[standard]",
    "pypdf>=6,<7",
    "pgvector>=0.4,<1",
]

[dependency-groups]
dev = [
    "pytest",
    "ruff",
]

[tool.pytest.ini_options]
pythonpath = ["src"]
testpaths = ["tests"]

[tool.ruff]
target-version = "py313"
line-length = 100

[tool.ruff.lint]
select = ["E", "F", "I", "UP", "B"]
