import { Activity, Database, FileText, LayoutDashboard, Settings, Telescope } from 'lucide-react'
import { NavLink, Outlet } from 'react-router-dom'
const navigation = [['Overview', LayoutDashboard], ['Observations', Telescope], ['Runs', Activity], ['Reports', FileText], ['Data Sources', Database], ['Settings', Settings]] as const
/** Renders the persistent ObserveAI product shell. */
export function AppShell() { return <div className="min-h-screen bg-[var(--color-background-canvas)] text-[var(--color-text-primary)] lg:flex"><aside className="bg-slate-950 px-4 py-6 text-slate-200 lg:min-h-screen lg:w-60"><div className="mb-8 px-3 text-xl font-semibold tracking-tight text-white">ObserveAI</div><nav aria-label="Main navigation" className="flex gap-1 overflow-x-auto lg:block lg:space-y-1">{navigation.map(([label, Icon]) => label === 'Observations' ? <NavLink key={label} to="/observations" className={({isActive}) => `flex shrink-0 items-center gap-3 rounded-lg px-3 py-2 text-sm ${isActive ? 'bg-slate-800 text-white' : 'hover:bg-slate-900 hover:text-white'}`}><Icon size={17} aria-hidden="true" />{label}</NavLink> : <span key={label} className="flex shrink-0 items-center gap-3 rounded-lg px-3 py-2 text-sm text-slate-400"><Icon size={17} aria-hidden="true" />{label}</span>)}</nav></aside><main className="min-w-0 flex-1 px-5 py-8 sm:px-8 lg:px-10"><Outlet /></main></div> }
