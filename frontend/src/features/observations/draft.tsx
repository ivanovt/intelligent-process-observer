/* eslint-disable react-refresh/only-export-components */
import { createContext, useContext, useEffect, useMemo, useReducer, useRef, useState, type ReactNode } from 'react'
import { getDefinitionCapabilities } from './api'
import type { AlertLensCreate, DefinitionCapabilities, MetricLensCreate, ObservationCreate } from './types'
export interface DraftAlert extends AlertLensCreate { clientKey:string }
export interface DraftMetric extends MetricLensCreate { clientKey:string }
export interface ObservationDraft { name:string; description:string; objective:string; lenses:DraftMetric[]; alert_lenses:DraftAlert[]; relationships:never[] }
export type DraftErrors=Record<string,string>
const blank=():ObservationDraft=>({name:'',description:'',objective:'',lenses:[],alert_lenses:[],relationships:[]})
type Action={type:'fresh'}|{type:'general';value:Pick<ObservationDraft,'name'|'description'|'objective'>}|{type:'upsert-alert';value:DraftAlert;key:'new'|string}|{type:'upsert-metric';value:DraftMetric;key:'new'|string}|{type:'clear'}
function replaceAtKey<T extends {clientKey:string}>(items:T[],value:T,key:string):T[] { if(key==='new')return [...items,value];const index=items.findIndex(item=>item.clientKey===key);return index<0?items:items.map((item,i)=>i===index?value:item) }
function reducer(state:ObservationDraft|null,action:Action):ObservationDraft|null { if(action.type==='fresh')return blank();if(action.type==='clear')return null;if(!state)return state;if(action.type==='general')return {...state,...action.value};if(action.type==='upsert-alert')return {...state,alert_lenses:replaceAtKey(state.alert_lenses,action.value,action.key)};return {...state,lenses:replaceAtKey(state.lenses,action.value,action.key)} }
export function validateAlert(value:DraftAlert,other:DraftAlert[]=[]):DraftErrors { const errors:DraftErrors={};if(!/^[a-z][a-z0-9_-]*$/.test(value.id))errors.id='Use a lowercase identifier.';if(!value.name.trim())errors.name='Name is required.';if(value.description!==null&&!value.description.trim())errors.description='Description cannot be blank.';if(!value.selector.query.trim())errors.query='Selector query is required.';if(other.some(item=>item.clientKey!==value.clientKey&&item.id===value.id))errors.id='Alert Lens ID must be unique.';if(value.analysis_objectives.some(item=>!item.trim())||new Set(value.analysis_objectives).size!==value.analysis_objectives.length)errors.objectives='Objectives must be non-blank and unique.';if(value.reference_periods.some(item=>!/^[1-9][0-9]*(m|h|d|w)$/.test(item))||new Set(value.reference_periods).size!==value.reference_periods.length)errors.references='Reference periods must be unique positive offsets.';return errors }
export function validateMetric(value:DraftMetric,other:DraftMetric[]=[]):DraftErrors { const errors:DraftErrors={};if(!/^[a-z][a-z0-9_-]*$/.test(value.id))errors.id='Use a lowercase identifier.';if(!value.name.trim())errors.name='Name is required.';if(value.description!==null&&!value.description.trim())errors.description='Description cannot be blank.';if(!value.metric_id.trim())errors.metric_id='Metric ID is required.';if(!value.source_id)errors.source_id='Select a configured source.';if(!value.query.trim())errors.query='Query is required.';if(!value.unit.trim())errors.unit='Unit is required.';if(value.analysis_objectives.some(item=>!['spike','drift','oscillation'].includes(item))||new Set(value.analysis_objectives).size!==value.analysis_objectives.length)errors.objectives='Use each supported objective at most once.';if(value.reference_periods.some(item=>!/^[1-9][0-9]*(m|h|d|w)$/.test(item))||new Set(value.reference_periods).size!==value.reference_periods.length)errors.references='Reference periods must be unique positive offsets.';if(other.some(item=>item.clientKey!==value.clientKey&&item.id===value.id))errors.id='Metric Lens ID must be unique.';return errors }
export function validateDraft(draft:ObservationDraft):DraftErrors { const errors:DraftErrors={};if(!draft.name.trim())errors.name='Name is required.';if(!draft.objective.trim())errors.objective='Objective is required.';if(draft.lenses.length+draft.alert_lenses.length===0)errors.aggregate='Configure at least one Lens.';draft.lenses.forEach((item,index)=>Object.entries(validateMetric(item,draft.lenses)).forEach(([field,message])=>errors[`lenses.${index}.${field}`]=message));draft.alert_lenses.forEach((item,index)=>Object.entries(validateAlert(item,draft.alert_lenses)).forEach(([field,message])=>errors[`alert_lenses.${index}.${field}`]=message));return errors }
export function serializeDraft(draft:ObservationDraft):ObservationCreate { return {name:draft.name,description:draft.description===''?null:draft.description,objective:draft.objective,lenses:draft.lenses.map(metric=>({id:metric.id,name:metric.name,description:metric.description,type:metric.type,metric_id:metric.metric_id,adapter_type:metric.adapter_type,source_id:metric.source_id,query:metric.query,unit:metric.unit,analysis_objectives:metric.analysis_objectives,reference_periods:metric.reference_periods})),alert_lenses:draft.alert_lenses.map(alert=>({id:alert.id,name:alert.name,description:alert.description,type:alert.type,source:alert.source,selector:alert.selector,analysis_objectives:alert.analysis_objectives,reference_periods:alert.reference_periods})),relationships:[]} }
type CapabilityState={status:'idle'|'pending'|'success'|'empty'|'failure';capabilities?:DefinitionCapabilities;error?:string}
interface DraftContextValue { draft:ObservationDraft|null; fresh:()=>void; clear:()=>void; updateGeneral:(value:Pick<ObservationDraft,'name'|'description'|'objective'>)=>void; upsertAlert:(value:DraftAlert,key:string)=>void; upsertMetric:(value:DraftMetric,key:string)=>void; capabilities:CapabilityState; loadCapabilities:()=>void }
const DraftContext=createContext<DraftContextValue|null>(null)
/** Owns the in-memory aggregate only while the create route branch is mounted. */
export function ObservationDraftProvider({children}:{children:ReactNode}) { const [draft,dispatch]=useReducer(reducer,null),[capabilities,setCapabilities]=useState<CapabilityState>({status:'idle'}),request=useRef<{id:number;controller:AbortController}|null>(null),sequence=useRef(0);useEffect(()=>()=>request.current?.controller.abort(),[]);const loadCapabilities=()=>{request.current?.controller.abort();const id=++sequence.current,controller=new AbortController();request.current={id,controller};setCapabilities({status:'pending'});getDefinitionCapabilities(controller.signal).then(result=>{if(request.current?.id!==id)return;const sources=result.metric.flatMap(adapter=>adapter.sources);setCapabilities(sources.length?{status:'success',capabilities:result}:{status:'empty',capabilities:result})}).catch(error=>{if(request.current?.id!==id||controller.signal.aborted)return;setCapabilities({status:'failure',error:error instanceof Error?error.message:'Unable to load Metric sources.'})})};const value=useMemo(()=>({draft,fresh:()=>dispatch({type:'fresh'}),clear:()=>dispatch({type:'clear'}),updateGeneral:(value:Pick<ObservationDraft,'name'|'description'|'objective'>)=>dispatch({type:'general',value}),upsertAlert:(value:DraftAlert,key:string)=>dispatch({type:'upsert-alert',value,key:key==='new'?'new':key}),upsertMetric:(value:DraftMetric,key:string)=>dispatch({type:'upsert-metric',value,key:key==='new'?'new':key}),capabilities,loadCapabilities}),[draft,capabilities]);return <DraftContext.Provider value={value}>{children}</DraftContext.Provider> }
/** Reads the nearest create-route draft provider. */
export function useObservationDraft(){const value=useContext(DraftContext);if(!value)throw new Error('ObservationDraftProvider is required');return value}
export const newAlert=(key:string):DraftAlert=>({clientKey:key,id:'',name:'',description:null,type:'alert',source:'jira_track_and_release',selector:{query:''},analysis_objectives:[],reference_periods:[]})
export const newMetric=(key:string):DraftMetric=>({clientKey:key,id:'',name:'',description:null,type:'metric',metric_id:'',adapter_type:'prometheus',source_id:'',query:'',unit:'',analysis_objectives:[],reference_periods:[]})
