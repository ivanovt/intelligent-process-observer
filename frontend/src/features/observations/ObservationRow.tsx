import { ArrowRight } from 'lucide-react'
import { Link } from 'react-router-dom'
import type { ObservationSummary } from './types'
/** Shows only supported summary data for an Observation definition. */
export function ObservationRow({observation}:{observation:ObservationSummary}) { const metrics=observation.lenses.length,alerts=observation.alert_lenses.length,relationships=observation.relationships.length; return <li className="grid gap-4 border-b border-[var(--color-border)] px-5 py-5 last:border-b-0 md:grid-cols-[minmax(0,1fr)_250px_auto] md:items-center"><div className="min-w-0"><h2 className="font-semibold">{observation.name}</h2>{observation.description?<p className="mt-1 text-sm text-[var(--color-text-secondary)]">{observation.description}</p>:null}</div><p className="text-sm text-[var(--color-text-secondary)]">{metrics} Metric {metrics===1?'lens':'lenses'}<br />{alerts} Alert {alerts===1?'lens':'lenses'} · {relationships} {relationships===1?'Relationship':'Relationships'}</p><Link className="inline-flex items-center gap-1 text-sm font-medium text-[var(--color-primary)] hover:underline" to={`/observations/${observation.id}`}>Open <ArrowRight size={16} aria-hidden="true" /></Link></li> }
